/**
 * The deployed API has no batch-RFQ endpoints. Kept as a no-op export for
 * compatibility with routes that previously imported the old local panel.
 */
export function BulkRfqSourcePanel() {
  return null;
}
